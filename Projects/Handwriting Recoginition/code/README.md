## Description

```
1) nnSciprt.py 
      is the file which contains all the code for getting the input from the dataset, processing and learning.
2) pickle file 
      contains the output written by pickle.dump(). It can be read by using pickle.load(params.pickle)
```
